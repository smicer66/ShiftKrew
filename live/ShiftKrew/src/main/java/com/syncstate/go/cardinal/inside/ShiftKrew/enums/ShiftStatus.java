package com.syncstate.go.cardinal.inside.ShiftKrew.enums;

public enum ShiftStatus {
    PENDING,
    EMPLOYEE_ARRIVED_FOR_SHIFT,
    EMPLOYER_STARTED_SHIFT,
    EMPLOYEE_CONFIRMED_START,
    EMPLOYEE_ENDED_SHIFT,
    EMPLOYER_ENDED_SHIFT,
    CANCELLED
}
